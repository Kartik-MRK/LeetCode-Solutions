from collections import dequeclass Solution:    def canVisitAllRooms(self, rooms: List[List[int]]) -> bool:        n=len(rooms)                visited=[False]*n        queue=deque([0])        # for ele in rooms[0]:        #     queue.append(ele)                while queue:            room=queue.popleft()            visited[room]=True        visited[0]=True            for ele in rooms[room]:                if not visited[ele]:                    queue.append(ele)                        for ele in visited:            if not ele:                return False        return True