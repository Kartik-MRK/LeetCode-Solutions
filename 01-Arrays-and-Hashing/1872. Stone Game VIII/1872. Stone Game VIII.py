class Solution:    def stoneGameVIII(self, stones: List[int]) -> int:        memo={}        prefix=[stones[0]]        n=len(stones)        for i in range(1,n):        def game(i):            if i==n-1:                return prefix[n-1]            take=prefix[i]-game(i+1)            skip=game(i+1)            memo[i]=max(take,skip)            if i in memo:                return memo[i]                return game(1)            return memo[i]            prefix.append(prefix[i-1]+stones[i])        